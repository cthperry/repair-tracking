# V161.112 變更摘要（功能優化）

## 功能優化
1. 維修列表新增「關鍵字搜尋」輸入框（客戶 / SN / 問題 / 單號）
   - 支援 debounce（避免每次按鍵都重算造成卡頓）
   - 記住上一次輸入（localStorage），重新整理後仍保留
   - 一鍵清除

2. 新增維修單預設值優化
   - 記住最近一次儲存的「優先級 / 產品線 / 設備」
   - 僅在「新增」模式套用，不影響編輯模式
   - 模板套用仍可正常覆寫欄位

## 版本
- BUILD_NUMBER：112
