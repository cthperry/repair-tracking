rules_version = '2';

// 本專案主要使用 Realtime Database。
// 若未啟用 Firestore，保守設定為全拒絕，避免誤以為已開放。

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
